// Keep the implementation available while the manual sync workflow is reviewed.
export const ENABLE_MANUAL_QUEST_SYNC = false;

// Removed quests are excluded from normal progression data. Enable this only
// when reviewing the upstream records in the quest list.
export const SHOW_REMOVED_QUESTS = false;
